#include<stdio.h>
int main()
{
    int x=-99;*ptr=&x;
    printf("value of var is %d add of var  is %p,x,&x);
    printf("\n tar value of ptr is %d tar add of ptr add is %p,x,&x);
    
    return 0;
}